services:
  - type: web
    name: hire-me-please
    env: node  # Explicitly setting the environment to Node.js
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: "18.x"
